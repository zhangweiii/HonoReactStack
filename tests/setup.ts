// tests/setup.ts
import '@testing-library/jest-dom';

// You can add other global setups here if needed
// For example, if you were using a specific mock strategy for a module:
// jest.mock('some-module', () => ({
//   // ... your mock implementation
// }));
